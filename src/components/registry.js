// Data shared throughout the component instances
export default {
	// Used by the scrollable component for native scrollbar type recognition
	nativeScrollbarWidth: null,

	// Latest registered browser identification, used by the scrollable component
	// to perform different native scrollbar hiding techniques
	browserId: null,
}
